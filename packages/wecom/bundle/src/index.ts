/** WeCom bundle layer, host half: a pure config patch with no runtime plugin body. */

/** Present for the build; the profile applies the bundle's cordis.patch.yml only. */
export function apply(): void {}
